@model IEnumerable<MovieStore.Core.Models.Response.MovieCardResponseModel>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<div class="container-fluid">
    <div class="row">
        @foreach (var m in Model)
        {
            @await Html.PartialAsync("_MovieCard", m)
        }
    </div>
</div>

